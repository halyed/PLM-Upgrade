apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: plm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/halyed/plm-upgrade-notification:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: plm-config
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: plm
spec:
  selector:
    app: notification-service
  ports:
    - port: 8085
      targetPort: 8085
