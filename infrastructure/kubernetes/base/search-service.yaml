apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: plm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      containers:
        - name: search-service
          image: ghcr.io/halyed/plm-upgrade-search:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: plm-config
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: plm
spec:
  selector:
    app: search-service
  ports:
    - port: 8083
      targetPort: 8083
