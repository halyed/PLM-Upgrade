# DO NOT commit real secrets — use sealed-secrets or AWS Secrets Manager in production.
# This is a template; values are base64-encoded placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: plm-secrets
  namespace: plm
type: Opaque
stringData:
  POSTGRES_USER: plm_user
  POSTGRES_PASSWORD: CHANGE_ME
  POSTGRES_DB: plm
  MINIO_ROOT_USER: minioadmin
  MINIO_ROOT_PASSWORD: CHANGE_ME
  KEYCLOAK_ADMIN: admin
  KEYCLOAK_ADMIN_PASSWORD: CHANGE_ME
  KC_DB_USER: keycloak
  KC_DB_PASSWORD: CHANGE_ME
