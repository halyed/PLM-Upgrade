apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-service
  namespace: plm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workflow-service
  template:
    metadata:
      labels:
        app: workflow-service
    spec:
      containers:
        - name: workflow-service
          image: ghcr.io/halyed/plm-upgrade-workflow:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: plm-config
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: plm
spec:
  selector:
    app: workflow-service
  ports:
    - port: 8082
      targetPort: 8082
